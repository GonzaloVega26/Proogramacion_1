#ifndef CALCULOS_H
#define CALCULOS_H
procedimiento cargarDatoVector(real[],entero);

procedimiento mostrarVector(real[], entero);

funcion real calcularMedia(real[], entero );
funcion real calcularDesviacionTipica(real[], entero );
#endif
